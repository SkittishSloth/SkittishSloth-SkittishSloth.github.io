<section>
    <% page.posts.each(function (post) { %>
        <article>
            <h2>
                <%= post.title %>
            </h2>

            <div>
                <%- post.content %>
            </div>
        
        </article>
    <% }) %>
</section>